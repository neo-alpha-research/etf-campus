name: Daily ETF data refresh

on:
  schedule:
    # Retry ladder on the morning AFTER each trading day (KST), so the prior
    # close lands as early as the official feed publishes it. The first run
    # that finds new data commits; later runs that day exit early as no-ops.
    # On-the-hour slots are the most congested on GitHub, so any single run may
    # fire late; the ladder is what absorbs that.
    #
    #   UTC (cron)      KST (local)          covers
    #   Mon-Fri 21:00   Tue-Sat 06:00        previous trading day close
    #   Mon-Fri 22:00   Tue-Sat 07:00
    #   Mon-Fri 23:00   Tue-Sat 08:00
    #   Tue-Sat 00:00   Tue-Sat 09:00
    #   Tue-Sat 02:00   Tue-Sat 11:00
    #   Tue-Sat 04:00   Tue-Sat 13:00        safety net
    - cron: "0 21 * * 1-5"
    - cron: "0 22 * * 1-5"
    - cron: "0 23 * * 1-5"
    - cron: "0 0 * * 2-6"
    - cron: "0 2 * * 2-6"
    - cron: "0 4 * * 2-6"
  workflow_dispatch:
    inputs:
      target:
        description: "Target date in YYYYMMDD (blank uses yesterday)"
        required: false
        type: string

permissions:
  contents: write

concurrency:
  group: daily-etf-data
  # Keep one refresh at a time. A manual retry must not cancel a nearly
  # completed data commit; the job-level timeout still bounds stalled runs.
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    # The collector is bounded to a small set of snapshot requests. Fail fast
    # rather than holding an Actions runner for most of an hour on API delays.
    timeout-minutes: 20

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Refresh official ETF data
        env:
          DATA_GO_KR_SERVICE_KEY: ${{ secrets.DATA_GO_KR_SERVICE_KEY }}
          KRX_OPEN_API_KEY: ${{ secrets.KRX_OPEN_API_KEY }}
          TARGET_DATE: ${{ inputs.target }}
        run: |
          # Scheduled runs always take the default path: newest available close,
          # falling back to the prior trading day. Only an explicit manual target
          # pins an exact date (and fails loudly when that date is not published).
          if [ -n "$TARGET_DATE" ]; then
            python scripts/update_daily_data.py --target "$TARGET_DATE" --require-exact-date
          else
            python scripts/update_daily_data.py
          fi

      - name: Refresh classification review draft
        run: python scripts/build_classification_review.py

      # The retry ladder means most runs find nothing new. Detect that here so
      # those runs skip npm ci / test / build entirely instead of burning ~8
      # minutes of Actions time per no-op.
      - name: Detect data changes
        id: changes
        run: |
          if git diff --quiet -- data/etf_master_draft.csv data/etf_returns_draft.csv data/pension_verify_sheet.csv data/classification; then
            echo "No data changes. Skipping validation and commit."
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "New data detected."
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi
          echo "Latest bas_dt: $(python -c "import csv;print(next(csv.DictReader(open('data/etf_master_draft.csv',encoding='utf-8-sig')))['bas_dt'])")"

      - name: Set up Node.js
        if: steps.changes.outputs.changed == 'true'
        uses: actions/setup-node@v4
        with:
          node-version-file: ".node-version"
          cache: npm

      - name: Install dependencies
        if: steps.changes.outputs.changed == 'true'
        run: npm ci

      - name: Validate refreshed data
        if: steps.changes.outputs.changed == 'true'
        env:
          NEXT_PUBLIC_SITE_URL: https://etf-campus.pages.dev
        run: |
          python -m unittest discover -s scripts/tests
          npm test
          npm run build

      - name: Commit changed data
        if: steps.changes.outputs.changed == 'true'
        run: |
          git config user.name "etf-campus-data-bot"
          git config user.email "etf-campus-data-bot@users.noreply.github.com"
          git add data/etf_master_draft.csv data/etf_returns_draft.csv data/pension_verify_sheet.csv data/classification
          git commit -m "data: refresh ETF snapshot"
          git push

      - name: Update D1 with incremental backfill
        if: steps.changes.outputs.changed == 'true'
        env:
          KRX_OPEN_API_KEY: ${{ secrets.KRX_OPEN_API_KEY }}
          DATA_GO_KR_SERVICE_KEY: ${{ secrets.DATA_GO_KR_SERVICE_KEY }}
        run: python scripts/backfill_api.py --days 10


      # A run can end green while the site quietly serves stale prices: if the
      # feed has not published, the collector falls back to the previous close
      # and reports no change. After the last rung of the ladder there is no
      # excuse left, so one missed trading day fails the job. GitHub emails the
      # repository owner when a scheduled workflow fails, which is the alert.
      #
      # Weekday market holidays live in data/market_holidays.txt. The first
      # unlisted holiday will trip this check once; add the date and it is
      # accurate again.
      - name: Assert data freshness
        if: github.event.schedule == '0 4 * * 2-6'
        run: python scripts/check_data_freshness.py
